### human_datetime_py 🐍
> Python bindings extension for human-datetime.d library.

* `uv venv venv_dir --python 3.13`
* `venv_dir/Scripts/activate` or `source venv_dir/bin/activate`
* `make` to use LDC2 compiler or `make dmd` to use DMD compiler
* `make ext`
